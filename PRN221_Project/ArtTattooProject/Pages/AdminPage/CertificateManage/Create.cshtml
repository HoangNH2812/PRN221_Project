@page
@model ArtTattooProject.Pages.AdminPage.CertificateManage.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Certificate</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Certificate.CertificateName" class="control-label"></label>
                <input asp-for="Certificate.CertificateName" class="form-control" />
                <span asp-validation-for="Certificate.CertificateName" class="text-danger"></span>
            </div>
            <div class="form-group small fw-bold mt-2 pt-1 mb-0">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>@Model.Msg</div>
<div>
    <a class="small fw-bold mt-2 pt-1 mb-0" asp-page="Index">Back</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
